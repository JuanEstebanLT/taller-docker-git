apiVersion: v1
kind: Pod
metadata:
  name: mi-primer-pod
  namespace: taller-k8s
  labels:
    app: nginx-standalone
    tier: frontend
spec:
  containers:
    - name: nginx
      image: nginx:1.25
      ports:
        - containerPort: 80
      env:
        - name: MI_VARIABLE
          value: "hola"
      resources:
        requests:
          memory: "64Mi"
          cpu: "250m"
        limits:
          memory: "128Mi"
          cpu: "500m"
